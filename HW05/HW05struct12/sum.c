#include <stdio.h>
#include <string.h>
#include <math.h>

int main(){
	int a, b, sum;
	printf("Nhap vao hai so tu nhien: ");
	scanf("%d %d", &a, &b);
	sum = a+b;
	printf("Tong 2 so tren la: %d", sum);
	return 0;
}
